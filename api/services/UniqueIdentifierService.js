module.exports = {
    get : function(cb){
        //UniqueIdentifier.findOne().exec(function(err, count){
        //cb(err, count);
    },
    update : function(id, cb){
        //UniqueIdentifier.update()
    }
}